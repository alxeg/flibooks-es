refresh_rate: 10 seconds
appenders:
  stdout:
    kind: console
    encoder:
      pattern: "{d(%+)(local)} {h({l})} [{t}] [{f}:{L}] {m}{n}"
root:
  level: trace
  appenders:
    - stdout
loggers:
  flibooks_es:
    level: debug
